Continuar el desarrollo dle tablero con indicadores de RRHH. Para ello implementar los siguientes puntos:

1. Cálculo de edad con yearfrac(). 

2. Nueva medida: promedio de edad.

3. Agregar KPI promedio de edad.

4. Agregar tooltips con mas datos. 

5. Crear columnas condicionales con DAX. Segmentar empleados por edad.

6. Visualizar en un gráfico de barras apiladas el grupo de edad. 

7. Crear columnas condicionales con DAX. Segmentar sueldos por monto. 

8. Crear columnas condicionales con DAX. Segmentar evaluación por notas. 

9. Agregar tooltips a gráfico de barras apiladas para grupos de sueldos y evaluación. 

10. Galería de temas. Importar json.

11. Aplicar formatos. 

Luego de estas implementaciones el tablero debería quedar con una interfaz similar a la siguiente:

Se adjunta: Fondo (SKU-DON empresa ficticia) y template descargado del sitio de powerBi.



grupo_edad = SWITCH(
    'Tabla Empleados'[Edad],
    'Tabla Empleados'[Edad] <= 20, 0-20,
    'Tabla Empleados'[Edad] <= 30, 20-30,
    'Tabla Empleados'[Edad] <= 40, 30-40,
    'Tabla Empleados'[Edad] <= 50, 40-50,
    'Tabla Empleados'[Edad] <= 60, 50-60,
)